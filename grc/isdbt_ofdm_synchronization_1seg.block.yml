# auto-generated by grc.converter

id: isdbt_ofdm_synchronization_1seg
label: OFDM Synchronization 1seg
category: ISDBT

parameters:
-   id: mode
    label: Transmission Mode
    dtype: enum
    default: '3'
    options: ['1', '2', '3']
    option_labels: [MODE1(2K), MODE2(4K), MODE3(8K)]
-   id: cp_length
    label: Guard Interval
    dtype: enum
    default: '0.125'
    options: ['0.250', '0.125', '0.0625', '0.03125']
    option_labels: [1/4, 1/8, 1/16, 1/32]

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ 108*2**(int(mode)-1) }
-   label: ch. taps
    domain: stream
    dtype: complex
    vlen: ${ 108*2**(int(mode)-1) }
    optional: true
-   label: freq. error
    domain: stream
    dtype: float
    optional: true
-   label: samp. error
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import isdbt
    make: isdbt.ofdm_synchronization_1seg(${mode}, ${cp_length})

file_format: 1

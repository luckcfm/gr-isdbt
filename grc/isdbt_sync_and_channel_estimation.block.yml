# auto-generated by grc.converter

id: isdbt_sync_and_channel_estimation
label: Sync And Channel Estimation
category: ISDBT

parameters:
-   id: transmission_mode
    label: Transmission Mode
    dtype: enum
    options: [2k, 4k, 8k]
    option_labels: [MODE1(2K), MODE2(4K), MODE3(8K)]
    option_attributes:
        fft_length: ['2048', '4096', '8192']
        payload_length: ['1405', '2809', '5617']
-   id: offset_max
    label: Maximun Frequency Offset
    dtype: int
    default: '200'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ transmission_mode.fft_length }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ transmission_mode.payload_length }
-   label: ch. taps
    domain: stream
    dtype: complex
    vlen: ${ transmission_mode.payload_length }
    optional: true
asserts:
- ${ offset_max > 0 }
- ${ offset_max < 500 }

templates:
    imports: import isdbt
    make: isdbt.sync_and_channel_estimation(${transmission_mode.fft_length}, ${transmission_mode.payload_length},
        ${offset_max})

file_format: 1

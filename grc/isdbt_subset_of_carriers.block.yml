# auto-generated by grc.converter

id: isdbt_subset_of_carriers
label: Subset of carriers
category: ISDBT

parameters:
-   id: total
    label: Total
    dtype: int
-   id: first
    label: First
    dtype: int
-   id: last
    label: Last
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ total }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ int(1+last-first) }

templates:
    imports: import isdbt
    make: isdbt.subset_of_carriers(${total}, ${first}, ${last})

file_format: 1

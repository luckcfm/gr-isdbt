# auto-generated by grc.converter

id: isdbt_time_deinterleaver
label: Time Deinterleaver
category: ISDBT

parameters:
-   id: mode
    label: Transmission Mode
    dtype: enum
    default: '3'
    options: ['1', '2', '3']
    option_labels: [MODE1(2K), MODE2(4K), MODE3(8K)]
-   id: segments_A
    label: Layer A Segments
    dtype: int
-   id: length_A
    label: Layer A Length
    dtype: int
-   id: segments_B
    label: Layer B Segments
    dtype: int
-   id: length_B
    label: Layer B Length
    dtype: int
-   id: segments_C
    label: Layer C Segments
    dtype: int
-   id: length_C
    label: Layer C Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 13*96*(2**(int(mode)-1)) }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ 13*96*(2**(int(mode)-1)) }
asserts:
- ${ segments_A + segments_B + segments_C == 13 }
- ${ segments_A > -1 }
- ${ segments_B > -1 }
- ${ segments_C > -1 }

templates:
    imports: import isdbt
    make: isdbt.time_deinterleaver(${mode}, ${segments_A}, ${length_A}, ${segments_B},
        ${length_B}, ${segments_C}, ${length_C})

file_format: 1

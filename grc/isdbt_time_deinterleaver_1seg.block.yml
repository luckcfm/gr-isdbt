# auto-generated by grc.converter

id: isdbt_time_deinterleaver_1seg
label: Time Deinterleaver 1-Seg
category: ISDBT

parameters:
-   id: mode
    label: Mode
    dtype: enum
    default: '3'
    options: ['1', '2', '3']
    option_labels: [MODE1(2K), MODE2(4K), MODE3(8K)]
-   id: length
    label: Length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 1*96*(2**(int(mode)-1)) }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ 1*96*(2**(int(mode)-1)) }

templates:
    imports: import isdbt
    make: isdbt.time_deinterleaver_1seg(${mode}, ${length})

file_format: 1
